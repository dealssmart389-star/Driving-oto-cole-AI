<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Driving-AI 2026 | المنصة المتكاملة</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #1a1a2e;
            --accent: #00d2ff;
            --glass: rgba(255, 255, 255, 0.1);
        }
        body {
            font-family: 'Cairo', sans-serif;
            background: radial-gradient(circle at top, #1e2a44, #0f172a);
            color: white;
            margin: 0;
            overflow-x: hidden;
        }
        .main-header {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(to bottom, rgba(26,26,46,0.8), transparent);
        }
        .main-header h1 { font-size: 3rem; margin: 0; font-weight: 900; color: var(--accent); }
        
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            padding: 40px;
            max-width: 1200px;
            margin: auto;
        }
        .app-card {
            background: var(--glass);
            backdrop-filter: blur(15px);
            border-radius: 30px;
            padding: 30px;
            border: 1px solid rgba(255,255,255,0.1);
            transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            text-decoration: none;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .app-card:hover {
            transform: translateY(-15px);
            background: rgba(255, 255, 255, 0.2);
            border-color: var(--accent);
            box-shadow: 0 20px 40px rgba(0,210,255,0.3);
        }
        .app-card i { font-size: 50px; margin-bottom: 20px; }
        .app-card h3 { margin: 10px 0; font-size: 1.5rem; }
        .app-card p { text-align: center; opacity: 0.8; font-size: 0.9rem; }
        
        .ai-status {
            position: fixed;
            top: 20px;
            right: 20px;
            background: #00ff88;
            color: #000;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
            box-shadow: 0 0 15px #00ff88;
        }
    </style>
</head>
<body>

<div class="ai-status">AI ACTIVE 2026</div>

<header class="main-header">
    <h1>Driving-AI Master</h1>
    <p>مرحباً بك في بوابة تعليم السياقة الأكثر تطوراً عالمياً</p>
</header>

<div class="dashboard-grid">
    <a href="Driving_AI_2026_Simulator.html" class="app-card">
        <i>🎮</i>
        <h3>محاكي السياقة</h3>
        <p>اختبر مهاراتك في بيئة تفاعلية تحاكي الواقع تماماً.</p>
    </a>

    <a href="Signs_Catalog_2026_Pro.html" class="app-card">
        <i>🛑</i>
        <h3>موسوعة البانوات</h3>
        <p>جميع الإشارات الدولية والمغربية مشروحة بالصوت والصورة.</p>
    </a>

    <a href="game.html" class="app-card">
        <i>🏎️</i>
        <h3>اللعبة التعليمية</h3>
        <p>تعلم قوانين السير من خلال تحديات ممتعة وسريعة.</p>
    </a>

    <a href="Driving_Exam_System.html" class="app-card">
        <i>📝</i>
        <h3>نظام الامتحانات</h3>
        <p>امتحانات تجريبية محدثة بناءً على مدونة السير 2026.</p>
    </a>
</div>

<script src="App_Optimizer.js"></script>
<script src="Driving_core_engine.js"></script>
<script src="Driving_AI_Adaptive_Core.js"></script>

<script>
    console.log("Welcome to Driving-AI Master Control.");
    // تفعيل وضع التكيف التلقائي من الملفات المرفوعة
    if(typeof DrivingAI_Core !== 'undefined') {
        console.log("Adaptive Core Loaded Successfully.");
    }
</script>

</body>
</html>
